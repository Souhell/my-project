{
  "name": "nodeconsoleapp2",
  "version": "1.0.0",
  "main": "app.js",
  "type": "commonjs",
  "description": "Automation app (Selenium now, Mocha infra ready)",
  "scripts": {
    "start": "node app.js",
    "lint": "eslint . --ext .js || echo 'Lint errors ignored'",
    "test": "mocha tests/**/*.js --timeout 20000 --reporter spec || echo 'No mocha tests found'"
  },
  "devDependencies": {
    "chai": "^6.0.1",
    "eslint": "^9.17.0",
    "mocha": "^11.7.2",
    "chromedriver": "^131.0.0",
    "selenium-webdriver": "^4.35.0"
  },
  "dependencies": {
    "axios": "^1.9.0"
  }
}
